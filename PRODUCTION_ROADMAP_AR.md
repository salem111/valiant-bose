# خطة تطوير المشروع — الصوت في المرحلة الأخيرة

## القرار
سيتم تأجيل تغيير طبقة الصوت من Agora إلى LiveKit حتى تنتهي بقية أعمال الإنتاج. لن نخلط بين تحديثات النظام والهجرة الصوتية حتى يبقى كل تغيير قابلًا للاختبار والتراجع.

## المرحلة الأولى: تثبيت الإنتاج بدون لمس الصوت
- فحص البناء وTypeScript والحزم.
- إنشاء بيئة إنتاج منفصلة عن التطوير.
- منع الأسرار من الظهور في الواجهة أو Git.
- مراجعة CORS وFirebase Authentication وJWT.
- إضافة فحص إعدادات الإنتاج قبل التشغيل.
- توحيد `PORT` و`NODE_ENV` وبيانات الخادم.
- إعداد health check والمراقبة والسجلات.

## المرحلة الثانية: حماية البيانات والعمليات المالية
- جعل العملات والألماسات والسحب معتمدة على Backend.
- إضافة idempotency لمنع تكرار الهدايا والتحويلات.
- منع تعديل الأرصدة ونتائج الألعاب من العميل.
- مراجعة قواعد RTDB وFirestore.
- اختبار حالات الرفض والاسترجاع وإعادة المحاولة.

## المرحلة الثالثة: الواجهة والوظائف
- إزالة بيانات Mock من مسارات الإنتاج.
- مراجعة الصلاحيات حسب Firebase UID فقط.
- تحسين حالات التحميل والأخطاء وإعادة الاتصال.
- تنظيف إعدادات Android وملفات الإنتاج.
- إضافة اختبارات قبول للغرف والبث والسحب والحسابات.

## المرحلة الأخيرة: LiveKit
بعد نجاح المراحل السابقة:
- إنشاء Adapter مستقل للصوت.
- تشغيل LiveKit في بيئة اختبار منفصلة.
- نقل الغرف الصوتية ثم البث المباشر.
- اختبار Android/Web على جهازين حقيقيين.
- إبقاء Agora كخطة رجوع مؤقتة حتى تثبت LiveKit.
- إزالة Agora فقط بعد نجاح اختبار القبول والإصدار التجريبي.

## بوابة النشر
لا يتم إطلاق النسخة العامة قبل نجاح:

- `npm run lint`
- `npm run build`
- فحص إعدادات الإنتاج.
- اختبار المصادقة والصلاحيات.
- اختبار عدم إمكانية تزوير الرصيد.
- اختبار السحب والـ refund.
- اختبار Android Release.
- اختبار الصوت بعد الانتهاء من المرحلة الأخيرة.
