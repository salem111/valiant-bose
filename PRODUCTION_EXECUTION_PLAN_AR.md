# خطة التنفيذ الاحترافية — SALEEM

## الوضع الحالي
الصوت سيبقى على Agora مؤقتًا. سيتم تأجيل الهجرة إلى LiveKit حتى تستقر بقية الأنظمة وتنجح اختبارات الإنتاج.

## المرحلة 1 — خط الأساس والبناء
**المخرجات:** نسخة يمكن بناؤها وتشغيلها بطريقة متكررة.

- تثبيت Node.js ونسخة الحزم.
- تشغيل `npm ci` من نسخة نظيفة.
- تشغيل `npm run lint` و`npm run build`.
- تشغيل `npm run preflight` على بيئة الإنتاج.
- تشغيل الخادم عبر `npm run start:production`.
- عدم نشر أي commit يفشل في هذه الخطوات.

## المرحلة 2 — أمن الخادم والهوية
**المخرجات:** كل endpoint حساس محمي بالهوية والحدود.

- منع الأسرار داخل Git وواجهة Vite.
- تضييق CORS إلى نطاقات الإنتاج فقط.
- استخدام Firebase UID الموثق بدل الاسم أو البيانات القادمة من العميل.
- إضافة health/readiness monitoring في منصة الاستضافة.
- تدوير أي مفاتيح حقيقية ظهرت في تاريخ المستودع.

## المرحلة 3 — قواعد البيانات والماليات
**المخرجات:** لا يمكن للعميل تزوير الرصيد أو تكرار العملية.

- نقل الهدايا والعملات والألماسات إلى Backend authoritative.
- إضافة idempotency لكل عملية مالية.
- استخدام transaction/ledger للخصم والإضافة.
- جعل Rocket وPK وLucky Bags خادمية المصدر.
- اختبار الرفض، انقطاع الشبكة، إعادة المحاولة، والـ refund.
- مراجعة Firebase Rules بواسطة Emulator/مشروع اختبار.

## المرحلة 4 — جودة الواجهة والتطبيق
**المخرجات:** تجربة مستقرة على Web وAndroid.

- إزالة Mock من أي مسار إنتاجي.
- توحيد حالات loading/error/empty/retry.
- اختبار تسجيل الدخول واستعادة الجلسة.
- اختبار Android back button والصلاحيات والصوت بالخلفية.
- إنشاء Release APK/AAB باستخدام Keystore خارج Git.

## المرحلة 5 — الصوت أخيرًا
**المخرجات:** قرار مبني على اختبار وليس تخمينًا.

- اختبار Agora الحالي على جهازين وشبكتين مختلفتين.
- إنشاء adapter مستقل للصوت.
- تنفيذ LiveKit في فرع تجريبي منفصل.
- نقل غرفة واحدة ثم البث المباشر.
- اختبار Android/Web، المتحدثين، المستمعين، الكتم، وإعادة الاتصال.
- إبقاء Agora كخطة رجوع حتى نجاح Beta.
- حذف Agora فقط بعد نجاح اختبارات القبول.

## بوابة Beta
لا تبدأ Beta قبل نجاح:

```bash
npm ci
npm run lint
npm run build
npm run preflight
```

ثم اختبار:

- المصادقة والصلاحيات.
- عدم تزوير العملات والألماسات.
- الهدايا والسحب والـ refund.
- Android Release.
- الصوت بين جهازين.

## القرار التشغيلي
لا يمكن إعلان المشروع جاهزًا تجاريًا من خلال الملفات وحدها. يلزم تشغيل حقيقي، أسرار بيئة صحيحة، قاعدة بيانات اختبار، وأجهزة فعلية. هذه الخطة تجعل كل مرحلة قابلة للقياس والتراجع بدل تغيير المشروع كله مرة واحدة.
