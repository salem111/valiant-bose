# تقرير تفعيل وإصلاح نظام السحب — SALEEM

تمت مراجعة مسار السحب في الواجهة والـ Backend وقاعدة PostgreSQL.

## ما تم إصلاحه

- تفعيل الاعتماد على Backend في إنشاء طلب السحب بدلاً من اعتبار حالة الواجهة مصدر الحقيقة.
- الهوية تؤخذ من Firebase/JWT الموثق، ورفض `userId` إذا خالف هوية التوكن.
- خصم الألماسات وفتح طلب `PENDING` يتمان داخل Transaction واحدة مع `FOR UPDATE`.
- منع أكثر من طلب سحب نشط للمستخدم عبر Unique Partial Index.
- تثبيت دورة الحالة:
  - `PENDING -> PROCESSING`
  - `PROCESSING -> SUCCESS`
  - `PENDING/PROCESSING -> REJECTED` مع Refund ذري.
- لا يمكن تعديل الحالات النهائية `SUCCESS` أو `REJECTED`.
- تأكيد `SUCCESS` يتطلب `payoutReference`.
- الرفض يتطلب سبباً، وإرجاع الألماسات يتم داخل نفس Transaction.
- إضافة قائمة بيضاء لبوابات السحب من السيرفر وعدم قبول بوابة مخترعة من العميل.
- التحقق من طول بيانات الحساب واسم المستفيد.
- تحقق أساسي من بريد PayPal/Payoneer وعنوان USDT TRC-20.
- التحقق من `statusFilter` في لوحة الإدارة بدلاً من تجاهل قيمة غير صالحة.
- وضع حدود لعدد السجلات المعادة: 100 لسجل المستخدم و500 للوحة الإدارة.
- لوحة الإدارة تستخدم Token المستخدم الحالي وتنفذ إجراءات المعالجة عبر Backend.
- إصلاح عرض مبلغ USD في لوحة الإدارة ليستخدم منزلتين عشريتين بدلاً من إضافة `.00` بشكل خاطئ.
- إزالة `.env` و`.env.local` من النسخة الموزعة حتى لا يتم تسريب أسرار البيئة.

## ملاحظات تشغيل مهمة

- يجب ضبط متغيرات البيئة الحقيقية على السيرفر، خصوصاً `AUTH_JWT_SECRET` وبيانات قاعدة البيانات وFirebase.
- يجب تشغيل migrations في بيئة Backend قبل استخدام السحب.
- السحب الفعلي إلى مزود الدفع ما زال عملية تشغيلية: `CONFIRM_SUCCESS` يسجل مرجع الدفع بعد أن ينفذ المسؤول التحويل لدى المزود.
- لا ينبغي اعتبار وجود زر "تأكيد النجاح" بحد ذاته تكاملاً آلياً مع PayPal/USDT/Zain Cash وغيرها؛ التكامل الآلي يحتاج API وcredentials لكل مزود.
- اختبار TypeScript الكامل في بيئة الفحص تعذر لأن `node_modules` غير موجودة داخل الأرشيف؛ لذلك ظهرت أخطاء Modules مفقودة، وليس أخطاء منطق السحب نفسها.
