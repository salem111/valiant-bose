# تقرير مراجعة SALEEM قبل النشر

## الحالة
تمت مراجعة نسخة المشروع المرفوعة بحثًا عن مشاكل الإنتاج، مع التركيز على Agora والصوت والفيديو والغرف والمقاعد والصلاحيات وPK وFirebase/Android.

> لا يمكن اعتبار التطبيق "100% جاهز للنشر" من فحص الملفات وحده؛ اختبار الصوت والفيديو الحقيقي يحتاج جهازين فعليين، جلسة Firebase حقيقية، وخدمة Agora Token تعمل من بيئة الإنتاج.

## إصلاحات منفذة في هذه النسخة

### Agora / الصوت
- توحيد توليد Agora UID بين العميل وخادم Token إلى نفس المجال `1..2,000,000,000`. كان العميل يستخدم مجالًا مختلفًا، مما قد يجعل خادم Token يرفض UID المطلوب ويؤدي لفشل الدخول/النشر.
- جعل Agora يملك Track الميكروفون في المسار الأساسي بدل إعادة استخدام Stream اختبار الصلاحيات.
- الإبقاء على AEC/ANS/AGC وجودة `high_quality` للمايك.
- عدم السماح بانتقال Audience إلى Host بدون Token publisher صالح.
- الاشتراك في الصوت البعيد عند `user-published` وتشغيله، مع إعادة محاولة autoplay عند تفاعل المستخدم.
- إزالة اعتماد مستوى الصوت على إنشاء/تعديل هوية المقعد عند كل volume event، لتقليل سباقات Firebase وتكرار العناصر.

### الموسيقى
- عزل الناشر: المستخدم الذي بدأ الموسيقى هو الذي ينشر Custom Audio Track عبر Agora؛ باقي الأجهزة لا تنشر نسخة ثانية.
- إضافة صلاحية RTDB تسمح لمشغل الموسيقى بتحديث حالة الموسيقى الخاصة به، بدل حصرها بالمالك فقط.
- YouTube لا يُعامل كملف صوت مباشر عبر HTMLAudio/Agora.

### المقاعد وشريط الأعضاء
- شريط الأعضاء يستخدم deduplication حسب `id` ويستبعد أي مستخدم موجود على مقعد.
- الدخول على المقعد ينقل المستخدم منطقيًا من قائمة الأعضاء إلى المقعد.
- إضافة قواعد RTDB لكتابة حضور المستخدم ومؤشر صوته ومقعده الشخصي بشكل منفصل.
- إضافة `ownerId` قياسي إلى حفظ الغرفة.

### الصلاحيات
- إزالة عدة bypasses ثابتة مبنية على IDs محددة، وإزالة الاعتماد على اسم المستخدم كدليل ملكية.
- ملكية الغرفة أصبحت مرتبطة بالـ ID/ownerId بدل الاسم أو مجرد الجلوس في المقعد الأول.
- تحدي PK من الواجهة أصبح متاحًا لصاحب الغرفة فقط.

### Android
- تمت مراجعة Manifest وForeground Service وWebView permission bridge.
- المشروع يستخدم Capacitor 7، compile/target SDK 35، وGradle 8.11.1 مع Android Gradle Plugin 8.7.2.
- أضيف `capacitor://localhost` إلى CORS الافتراضي لخدمة Token.

### قابلية تثبيت المشروع
- إزالة dependency ذات مسار Windows محلي `react-example: file:C:/Users/...` من `package.json` و`package-lock.json` لأنها غير صالحة كاعتماد نشر على جهاز/بيئة أخرى.

## مشاكل إنتاجية ما زالت تحتاج قرار/اختبار

### 1. Agora Token Service
يجب ضبط `VITE_SERVER_API_BASE_URL` في بيئة الإنتاج بحيث يصل التطبيق إلى `server.ts`، ويجب ضبط `AGORA_APP_CERTIFICATE` على الخادم فقط. بدون ذلك لا يمكن ضمان دخول Agora أو النشر.

### 2. PK بين المستخدمين
حالة PK تُحفظ في `room_pk`. إنشاء الجولة/إنهاؤها محميان للمالك، لكن تحديث نقاط PK الحالي ما زال ينفذ من العميل. هذا ليس نموذجًا آمنًا نهائيًا لأن خادمًا موثوقًا يجب أن يتحقق من قيمة الهدية/الخصم ويحسب النقاط. لا ينبغي اعتماد PK المالي كنظام production-authoritative قبل نقل احتساب النقاط إلى backend.

### 3. المطور/المشرف
الواجهة تعرف المشرفين، لكن قواعد RTDB الحالية لا تمنح المشرف صلاحية كتابة جذر الغرفة. لذلك أي عملية مشرف تعتمد على `saveRoomToRealtimeDb` قد لا تستمر على الخادم. الحل الإنتاجي الصحيح هو endpoints/server-authoritative actions للميوت، الطرد، القفل، إدارة المقاعد والصلاحيات.

### 4. المطابقة العشوائية في PK
`PKSetupModal` يحتوي على matchmaking تجريبي يولد غرفة عشوائية محليًا. هذا ليس matchmaking حقيقيًا بين غرف حية، ولذلك لا ينبغي تقديمه للمستخدمين كخدمة إنتاجية قبل ربطه بخدمة مطابقة فعلية.

### 5. اختبار البناء
تعذر تشغيل `npm ci`/تثبيت الحزم ضمن بيئة الفحص بسبب انتهاء مهلة الشبكة، لذلك لم أزعم نجاح `tsc` أو `vite build`. كذلك اختبارات التدقيق المرفقة لم تعمل لأن `node_modules` لم تكتمل.

## اختبار قبول إلزامي قبل Google Play

1. جهاز A يدخل غرفة عامة، جهاز B يدخل نفس الغرفة.
2. A يجلس ويتكلم: B يسمع A.
3. B يجلس ويتكلم: A يسمع B.
4. كتم/فتح المايك من الطرفين.
5. A يشغل ملف صوت مباشر: B يسمعه مرة واحدة.
6. B يشغل ملف صوت مباشر: A يسمعه مرة واحدة.
7. تجربة الصوت + الموسيقى معًا.
8. تجربة الدخول/الخروج من المقعد عدة مرات والتأكد من عدم تكرار المستخدم.
9. التأكد أن المستخدم يظهر في شريط الأعضاء قبل المقعد، وبعد الصعود يظهر في المقعد فقط.
10. اختبار مالك الغرفة/مشرف/عضو على نفس الغرفة والتأكد من أن كل صلاحية تعمل وتُحفظ على السيرفر.
11. اختبار البث المرئي بين جهاز مضيف ومشاهد، ثم تجربة PK بعد ربطه بالـ backend الموثوق.
12. اختبار Android بعد `npm install` ثم `npm run build` ثم `npx cap sync android` ثم Release build.
