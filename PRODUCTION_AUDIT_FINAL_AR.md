# تقرير فحص SALEEM — مراجعة ما قبل النشر

تاريخ الفحص: 2026-08-30

## ما تم فحصه
- الغرفة الصوتية وAgora UID/roles/publish/subscribe/autoplay.
- البث المباشر والصوت/الفيديو ومسار التوكن.
- حضور الغرف والمقاعد والإشارات وطلبات الصعود.
- PK وتحديث النقاط وتحديات PK.
- الهدايا والأرصدة ومسار الألماسات.
- السحب وواجهة السحب وBackend الخاص بالسحب.
- صلاحيات RTDB وFirestore.
- Rocket/الألعاب والـreactions.
- بيانات Mock/Simulation التي قد تظهر في نسخة الإنتاج.
- TypeScript/build dependency availability.

## إصلاحات نُفذت
1. توحيد Agora numeric UID بين العميل والخادم إلى نفس نطاق 1..2,000,000,000.
2. إزالة heuristic خطير كان يحذف أي متحدث معرفه يبدأ بحروف معينة؛ واستبداله بإزالة تكرار حقيقية حسب userId داخل المقاعد.
3. إيقاف حقن MOCK_ROOMS في قائمة الغرف الإنتاجية من App.
4. إضافة قواعد RTDB للمسارات التي كانت موجودة في الكود لكن مفقودة من القواعد، ومنها:
   - live_streams
   - join_requests
   - challenge_requests
   - room_cinema
   - room_pk (موجودة مع حماية المالك)
   - rooms/messages
   - rooms/signals
   - game_rocket
5. إضافة حماية ownership/identity لرسائل الغرف وإشارات WebRTC وطلبات التحدي.
6. حماية bets في Rocket بحيث لا يكتب bet إلا صاحب userId الموجود داخل الرهان.
7. حماية endpoint الخاص بـ combined diamond conversion: أصبح يتطلب Firebase ID token ويتحقق أن userId يطابق الهوية الموثقة.
8. الحفاظ على السحب الحقيقي في Backend كمسار مالي authoritative مع transaction وقيد unique لطلب سحب نشط.

## نتائج مهمة قبل النشر
### 🔴 حرجة — تحتاج Backend مالي موحد
الهدايا في `App.tsx` ما زالت تخصم العملات في state المحلي، بينما تحديث أرصدة الألماسات للمتلقي يعتمد على عميل Firebase. قواعد RTDB الحالية تمنع المستخدم من تعديل رصيد مستخدم آخر، وهذا صحيح أمنياً، لكنه يعني أن نظام الهدية الحالي ليس نظاماً مالياً production-grade.

الحل النهائي المطلوب: Gift Transaction API موثق بالتوكن، مع transaction/ledger في PostgreSQL، يخصم عملات المرسل ويضيف ألماسات المتلقي مرة واحدة باستخدام idempotency key، ثم يعيد الرصيدين للعميل.

### 🔴 حرجة — Rocket/الألعاب ذات العملات
`placeRocketBetServer` و`cashoutRocketBetServer` موجودان في `firebase.ts` لكن التنفيذ الحالي ما زال يعتمد على RTDB من العميل ولا توجد محاسبة مالية authoritative كاملة. لا ينبغي اعتبار اللعبة المالية آمنة للنشر التجاري قبل نقل الرهان والكاش آوت إلى Backend transaction.

### 🔴 حرجة — PK
تحديث PK score الحالي يتم من العميل، وقواعد `room_pk` تقصر الكتابة على مالك الغرفة. هذا يمنع بعض تحديثات PK من المتابعين، وهو أفضل أمنياً من السماح لأي مستخدم بتغيير النقاط، لكنه يعني أن PK المالي الحقيقي يحتاج خدمة Backend تجمع أحداث الهدايا وتحدث النتيجة بشكل موثوق.

### 🟠 متوسطة — البث المباشر
مسار Agora الخاص بالمذيع موجود ويستخدم token publisher، والاشتراك بالصوت موجود. لكن الاختبار الحقيقي لجهازين/شبكتين لا يمكن إثباته من static inspection فقط؛ يلزم اختبار Android حقيقي مع جهازين.

### 🟠 متوسطة — Build
لم يتمكن الفحص الحالي من تنزيل كل npm dependencies بسبب مهلة/عدم توفر بعض الحزم في cache، لذلك لم يتم اعتماد نتيجة `npm run build` كنجاح نهائي.

## اختبار الجهازين المطلوب قبل Google Play
1. جهاز A يدخل نفس الغرفة مع B.
2. A يصعد مقعداً ويتكلم → B يسمع.
3. B يصعد مقعداً ويتكلم → A يسمع.
4. كتم/فتح المايك.
5. الخروج وإعادة الدخول.
6. دخول المضيف لا ينتج مقعدين أو صورتين.
7. المستخدم الموجود على المقعد لا يبقى مكرراً في شريط الأعضاء.
8. تشغيل أغنية من A → B يسمعها، والعكس.
9. بدء فيديو Live → جهاز المشاهد يسمع ويرى.
10. طلب صعود Live → قبول/رفض.
11. إرسال Gift → التحقق من الخصم والإضافة مرة واحدة.
12. PK Gift → النقاط لا يمكن تزويرها من DevTools.
13. Withdrawal → لا يتم الخصم إلا بعد نجاح Backend transaction.
14. رفض السحب → refund مرة واحدة فقط.
15. إعادة إرسال نفس request لا ينشئ عملية مالية ثانية.

## الحكم
النسخة أصبحت أنظف وأقوى من ناحية Agora/RTDB والهوية، وتم إصلاح عدة أسباب مباشرة للمشاكل التي كانت ستمنع أو تعطل وظائف الغرف والبث.

لكن لا يمكن وصفها بأنها "100% جاهزة للنشر التجاري" طالما أن الهدايا وRocket/PK المالي ليست كلها authoritative في Backend. هذه ليست مشكلة شكلية؛ هي حماية من التلاعب بالأرصدة والنقاط.
